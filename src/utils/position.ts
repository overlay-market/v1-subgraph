import { Position } from './../../generated/schema';
import { Address, BigInt, ethereum } from '@graphprotocol/graph-ts'

// @TO-DO: create function to retrieve position by market address + positionId
// function getPositionByMarket(event: ethereum.Event, positionId: BigInt): Position | null {}